{
  "name": "succubus-state-tracker",
  "version": "5.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check index.js && node --check src/*.js"
  }
}
